<div class="container">
  <div class="d-flex">
    <h1>Tasks</h1>
  </div>
  <ul class="list">
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task) %></li>
    <% end %>
  </ul>
</div>
